import React from "react";

const Header = () => {
    return (
        <header className="header">
            <div>logo</div>
            <div>links?</div>
        </header>
    );
};

export default Header;
